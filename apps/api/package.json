{
  "name": "@pigo/api",
  "private": true,
  "scripts": {
    "build": "OUTPUT=\"${VERCEL_OUTPUT_FILE:-dist/pigo-api}\"; mkdir -p \"$(dirname \"$OUTPUT\")\" && go build -o \"$OUTPUT\" .",
    "dev": "air .",
    "test": "go test ./...",
    "check": "go vet ./...",
    "fix": "go fmt ./..."
  }
}
